#include <stdio.h>
#include <

typedef struct {
B
typedef struct {
	int easy_ope_


typedef enum {
	ADD = 0,
	SUB,
	MUL,
	MOD,
	QUIZ,
} QuestionKind;

typedef struct {
	union {
		int left;
		int right;
		

typedef struct {
	QuestionKind kind;
	

